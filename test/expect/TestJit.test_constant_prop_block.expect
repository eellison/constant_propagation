graph(%input_tensor : Dynamic) {
  %1 : Long() = prim::Constant[value={3}]()
  %2 : Long() = prim::Constant[value={2}]()
  %3 : Dynamic = aten::lt(%input_tensor, %2)
  %c : Dynamic = prim::If(%3)
    block0() {
      %c.1 : Dynamic = aten::sub[alpha={1}](%input_tensor, %1)
      -> (%c.1)
    }
    block1() {
      %c.2 : Dynamic = aten::add[alpha={1}](%input_tensor, %1)
      -> (%c.2)
    }
  return (%c);
}
